faster-whisper>=1.2
sounddevice>=0.4
keyboard>=0.13
pyperclip>=1.8
numpy>=1.24
pystray>=0.19
Pillow>=10.0
anthropic>=0.40
pywebview>=6.0

# GPU-Beschleunigung (optional, aber stark empfohlen fuer DE-Genauigkeit):
# liefern cuBLAS + cuDNN als DLLs fuer CTranslate2/faster-whisper, ohne dass das
# volle CUDA-Toolkit installiert sein muss. Ohne GPU/DLLs faellt die App still auf
# CPU zurueck (kleineres Modell). Passend zu CTranslate2 4.x: CUDA 12 + cuDNN 9.
nvidia-cuda-runtime-cu12
nvidia-cublas-cu12
nvidia-cudnn-cu12>=9,<10
